# ************************** SqlDBM: Snowflake *************************
# ******** Generated by SqlDBM: commercial_aviation by jerdavies *******


version: 2

models:
  - name: AIRCAFT
    description: A table that stores comprehensive information about aircraft, including details such as aircraft model, manufacturer, registration number, type, capacity, operational status, and other relevant technical and identification attributes used for tracking and managing aviation assets.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - AIRCRAFT_ID
        name: PK_1
      - type: foreign_key
        columns:
          - AIRLINE_ID
        expression: CORE.AIRLINE (AIRLINE_ID)
        name: FK_1
    columns:
      - name: AIRCRAFT_ID
        data_type: integer
        description: Unique identifier assigned to each individual aircraft in the database, used to distinctly track and reference specific aircraft across various records and operations.
        constraints:
          - type: not_null
      - name: REGISTRATION_NUMBER
        data_type: varchar(16777216)
        description: A unique identifier assigned to an entity during its registration process, typically used for official tracking and identification purposes. This number serves as a distinct reference code that helps in uniquely distinguishing and documenting the specific record within the system.
        constraints:
          - type: not_null
      - name: MODEL_TYPE
        data_type: varchar(16777216)
        description: Identifies the specific type or category of model represented in the dataset, which could include variations like machine learning models, statistical models, predictive models, or classification models used in the analysis or research.
        constraints:
          - type: not_null
      - name: MODEL_VARIANT
        data_type: varchar(16777216)
        description: Identifies the specific configuration or trim level of a vehicle model, representing the unique combination of features, equipment, and specifications within a particular vehicle line
        constraints:
          - type: not_null
      - name: MANUFACTURER
        data_type: varchar(16777216)
        description: The Manufacturer column identifies the company or brand that produced the product or item in the table. It provides information about the origin and creator of the specific item, which can be useful for tracking product lineage, quality assessment, or filtering and analyzing data based on different manufacturers.
        constraints:
          - type: not_null
      - name: AIRLINE_ID
        data_type: integer
        description: Unique identifier assigned to each airline in the database, typically used to uniquely distinguish and reference specific airlines across different records or tables.
        constraints:
          - type: not_null

  - name: AIRLINE
    description: A table that stores comprehensive information about airlines, including their unique identifiers, names, country of origin, hub locations, founding year, and operational details such as fleet size, primary routes, and contact information.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - AIRLINE_ID
        name: PK_1
    columns:
      - name: AIRLINE_ID
        data_type: integer
        description: Unique identifier assigned to an airline in the database, used to distinctly represent and reference each specific airline entry
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(16777216)
        description: Represents the unique name for the Airline
        constraints:
          - type: not_null

  - name: AIRPORT
    description: A table representing airports, containing essential information about airport locations, identifiers, and basic characteristics. This table likely includes fields such as airport code, name, city, country, geographic coordinates (latitude and longitude), and potentially additional details like airport type, elevation, and operational status.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - AIRPORT_ID
        name: PK_1
      - type: foreign_key
        columns:
          - COUNTRY_ID
        expression: CORE.COUNTRY (COUNTRY_ID)
        name: FK_3
    columns:
      - name: AIRPORT_ID
        data_type: integer
        description: Unique identifier for an airport, typically a code that uniquely represents a specific airport location within a database or transportation system.
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(16777216)
        description: Represents the unique name identifier for an entity in the table with ID 8c0c29b3-f090-4252-9d25-db526aa73f06, typically used to provide a human-readable label or title for the specific record.
        constraints:
          - type: not_null
      - name: LATITUDE
        data_type: number(38,0)
        description: Geographic coordinate representing the north-south position of a point on the Earth's surface, measured in degrees from the equator (positive values indicate north of the equator, negative values indicate south of the equator).
        constraints:
          - type: not_null
      - name: LONGITUDE
        data_type: number(38,0)
        description: Geographic coordinate representing the east-west position on the Earth's surface, measured in degrees from the prime meridian. Negative values indicate western longitude, while positive values indicate eastern longitude.
        constraints:
          - type: not_null
      - name: ELEVATION
        data_type: number(38,0)
        description: Represents the height above sea level for a specific geographic location, typically measured in meters or feet. This column provides the vertical distance from a reference point (usually mean sea level) to the ground surface of the given location.
        constraints:
          - type: not_null
      - name: COUNTRY_ID
        data_type: integer
        description: Unique identifier for each country in the database, serving as the primary key to uniquely distinguish and reference individual countries within the table.
        constraints:
          - type: not_null

  - name: BOOKING
    description: A table that stores booking information, capturing details of reservations or appointments made by customers. It likely includes key fields such as booking ID, customer details, service or item being booked, date and time of booking, status of the reservation, and any additional notes or special requirements.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - BOOKING_ID
        name: PK_1
      - type: foreign_key
        columns:
          - PASSENGER_ID
        expression: CORE.PASSENGER (PASSENGER_ID)
        name: FK_1
    columns:
      - name: BOOKING_ID
        data_type: integer
        description: Unique identifier for each booking transaction, used to track and reference individual bookings within the system. This alphanumeric code helps in uniquely identifying and managing specific booking records.
        constraints:
          - type: not_null
      - name: TOTAL_AMOUNT
        data_type: number(38,0)
        description: The total monetary amount representing the complete sum of all financial transactions or line items in the given record or entry. This column aggregates all costs, charges, or values associated with the specific transaction or data point.
        constraints:
          - type: not_null
      - name: BOOKING_CURRENCY
        data_type: varchar(3)
        description: A standardized code representing the specific currency used in financial transactions or reporting for this table's records. Typically follows international standards like ISO 4217, which provides unique three-letter alphabetic codes for currencies worldwide (e.g., USD for US Dollar, EUR for Euro, GBP for British Pound).
        constraints:
          - type: not_null
      - name: BAG_COUNT
        data_type: integer
        description: Bag count for passenger associated with this booking
        constraints:
          - type: not_null
      - name: CREATED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the exact date and time when the record was initially created in the system, used for tracking the origin point of the entry
        constraints:
          - type: not_null
      - name: UPDATED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the most recent date and time when the record was last updated or modified in the system
        constraints:
          - type: not_null
      - name: PASSENGER_ID
        data_type: integer
        description: Unique identifier assigned to each passenger in the dataset, used to distinctly track and reference individual travelers within the system or record.
        constraints:
          - type: not_null

  - name: COUNTRY
    description: A comprehensive table storing information about countries, including their unique identifiers, names, geographical details, and other relevant attributes used for global reference and classification in database systems.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - COUNTRY_ID
        name: PK_1
    columns:
      - name: COUNTRY_ID
        data_type: integer
        description: Unique identifier for each country in the database, used to uniquely reference and distinguish different countries within the table or across related tables
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(16777216)
        description: Name column representing the unique identifier or title for an entry in the table, typically used to provide a human-readable label or reference for the specific record.
        constraints:
          - type: not_null
      - name: COUNTRY_ISO_CODE
        data_type: varchar(16777216)
        description: A standardized two or three-letter code representing a specific country according to international standards (such as ISO 3166-1 alpha-2 or alpha-3), used for uniquely identifying and categorizing countries in databases, geographic systems, and international communications.
        constraints:
          - type: not_null

  - name: FLIGHT
    description: A Flight table represents detailed information about airline transportation, including flight identifiers, routing, scheduling, aircraft details, and passenger-related information. It typically captures essential data such as flight number, departure and arrival locations, scheduled and actual times, aircraft type, flight status, and other relevant operational parameters for tracking and managing air travel.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - FLIGHT_DETAIL_ID
        name: PK_1
      - type: foreign_key
        columns:
          - FLIGHT_SCHEDULE_ID
        expression: CORE.FLIGHT_SCHEDULE (FLIGHT_SCHEDULE_ID)
        name: FK_1
      - type: foreign_key
        columns:
          - ROUTE_ID
        expression: CORE.ROUTE (ROUTE_ID)
        name: FK_2
      - type: foreign_key
        columns:
          - AIRCRAFT_ID
        expression: CORE.AIRCAFT (AIRCRAFT_ID)
        name: FK_3
      - type: foreign_key
        columns:
          - GATE_ID_1
        expression: CORE.GATE (GATE_ID)
        name: FK_4
      - type: foreign_key
        columns:
          - GATE_ID
        expression: CORE.GATE (GATE_ID)
        name: FK_5
      - type: foreign_key
        columns:
          - TICKET_ID
        expression: CORE.TICKET (TICKET_ID)
        name: FK_6
    columns:
      - name: FLIGHT_DETAIL_ID
        data_type: integer
        description: Unique identifier for a specific flight detail record, used to uniquely track and reference individual flight information within the database table
        constraints:
          - type: not_null
      - name: ACTUAL_ARRIVED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the actual time when an aircraft arrived at its destination, used to track precise arrival timing and potentially compare against expected or scheduled arrival times.
        constraints:
          - type: not_null
      - name: ACTUAL_DEPARTURE_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the actual time when a departure occurred, representing the precise moment the aircraft left its original location
        constraints:
          - type: not_null
      - name: CREATED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating when the record was initially created in the system, representing the exact date and time of the record's first entry
        constraints:
          - type: not_null
      - name: UPDATED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the last modification time of the record, automatically updated when any changes are made to the entity
        constraints:
          - type: not_null
      - name: FLIGHT_STATUS
        data_type: varchar(16777216)
        description: Indicates the current condition or state of a flight, such as on-time, delayed, cancelled, departed, arrived, or in-progress. This column provides real-time or historical status information about the flight's operational condition.
        constraints:
          - type: not_null
      - name: CANCELLATION_REASON
        data_type: varchar(16777216)
        description: Indicates the specific reason why a reservation, booking, or service was cancelled by the customer or the service provider. This column captures the underlying cause for the cancellation, which can help in understanding customer behavior, improving service quality, and identifying potential issues in the booking or cancellation process.
        constraints:
          - type: not_null
      - name: ROUTE_ID
        data_type: integer
        description: Unique identifier for a specific transportation route, used to distinctly label and track individual routes within a transportation system or network.
        constraints:
          - type: not_null
      - name: AIRCRAFT_ID
        data_type: integer
        description: Unique identifier assigned to each individual aircraft in the database, used to distinctly track and reference specific aircraft across various records and operations.
        constraints:
          - type: not_null
      - name: FLIGHT_SCHEDULE_ID
        data_type: integer
        description: Unique identifier for a specific flight schedule entry, used to track and reference individual flight schedules within the system. This column helps in uniquely identifying and managing different flight schedule records.
        constraints:
          - type: not_null
      - name: GATE_ID
        data_type: varchar(16777216)
        description: Unique identifier for the specific airport gate where an arriving flight will land and passengers will disembark
        constraints:
          - type: not_null
      - name: GATE_ID_1
        data_type: varchar(16777216)
        description: Unique identifier for the departure gate from which an aircraft is scheduled to depart, used to track and manage gate assignments in an airport terminal
        constraints:
          - type: not_null
      - name: TICKET_ID
        data_type: integer
        description: ''
        constraints:
          - type: not_null

  - name: FLIGHT_SCHEDULE
    description: A comprehensive table tracking flight schedules, including details such as flight numbers, departure and arrival times, routes, aircraft information, and operational status for airlines and airport management.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - FLIGHT_SCHEDULE_ID
        name: PK_1
      - type: foreign_key
        columns:
          - ROUTE_ID
        expression: CORE.ROUTE (ROUTE_ID)
        name: FK_1
    columns:
      - name: FLIGHT_SCHEDULE_ID
        data_type: integer
        description: Unique identifier for a specific flight schedule entry, used to track and reference individual flight schedule records in the database
        constraints:
          - type: not_null
      - name: FLIGHT_NUMBER
        data_type: varchar(16777216)
        description: Unique identifier for a specific flight, typically consisting of an airline code and a sequential or random number that distinguishes this particular flight from others. The Flight Number helps in tracking, booking, and referencing individual flights in airline and travel systems.
        constraints:
          - type: not_null
      - name: DEPARTURE_TIME
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the planned or scheduled time of departure for a specific event or transportation service, representing the exact moment when a vehicle, flight, or other mode of transportation is expected to leave its origin point
        constraints:
          - type: not_null
      - name: ARRIVAL_DATE
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the planned or scheduled arrival time for a specific event, flight, transportation, or delivery
        constraints:
          - type: not_null
      - name: ROUTE_ID
        data_type: integer
        description: Unique identifier for a specific transportation route, used to distinctly label and track individual routes within a transportation system or network.
        constraints:
          - type: not_null

  - name: GATE
    description: A gate is a sphysical location at an airport and is usually given a num eric identifiertr
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - GATE_ID
        name: PK_1
      - type: foreign_key
        columns:
          - AIRPORT_ID
        expression: CORE.AIRPORT (AIRPORT_ID)
        name: FK_1
    columns:
      - name: GATE_ID
        data_type: varchar(16777216)
        description: Unique identifier for a specific gate at the airport (within a specific terminal)
        constraints:
          - type: not_null
      - name: AIRLINE_ID
        data_type: integer
        description: Unique identifier assigned to an airline in the database, used to distinctly represent and reference each specific airline entry
        constraints:
          - type: not_null
      - name: TERMINAL
        data_type: varchar(16777216)
        description: A terminal representing a specific zone in an airport, for exsmple Terminal 3 or South Terminal. These tend to be more common in larger, internastional airports.
        constraints:
          - type: not_null
      - name: AIRPORT_ID
        data_type: integer
        description: Unique identifier for an airport, typically a code that uniquely represents a specific airport location within a database or transportation system.
        constraints:
          - type: not_null

  - name: PASSENGER
    description: Representing passengers in a transportation system, storing essential information about individuals who travel, including personal details, contact information, and potential additional attributes related to their travel profile.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - PASSENGER_ID
        name: PK_1
      - type: foreign_key
        columns:
          - COUNTRY_ID
        expression: CORE.COUNTRY (COUNTRY_ID)
        name: FK_1
    columns:
      - name: PASSENGER_ID
        data_type: integer
        description: Unique identifier assigned to each passenger in the dataset, used to distinguish and track individual passengers across different records or analyses
        constraints:
          - type: not_null
      - name: FIRST_NAME
        data_type: varchar(16777216)
        description: A personal identifier representing an individual's first name, typically used to capture the initial or given name of a person in a database or record system.
        constraints:
          - type: not_null
      - name: LAST_NAME
        data_type: varchar(16777216)
        description: Last Name column storing the surname or family name of an individual in the database table. Typically contains the family or inherited name of a person, used for identification and sorting purposes.
        constraints:
          - type: not_null
      - name: PASSPORT_NUMBER
        data_type: integer
        description: A unique identifier assigned to an individual's passport, typically consisting of a series of alphanumeric characters used for international travel and personal identification purposes.
        constraints:
          - type: not_null
      - name: COUNTRY_ID
        data_type: integer
        description: Unique identifier for each country in the database, used to uniquely reference and distinguish different countries within the table or across related tables
        constraints:
          - type: not_null

  - name: ROUTE
    description: A Route represents a specific travel path or transportation connection between two or more locations, typically used in mapping, navigation, or transportation systems. It includes details about the origin, destination, distance, mode of transportation, and potentially intermediate waypoints or stops along the journey.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - ROUTE_ID
        name: PK_1
      - type: foreign_key
        columns:
          - AIRLINE_ID
        expression: CORE.AIRLINE (AIRLINE_ID)
        name: FK_1
      - type: foreign_key
        columns:
          - AIRCRAFT_ID
        expression: CORE.AIRCAFT (AIRCRAFT_ID)
        name: FK_2
      - type: foreign_key
        columns:
          - DEPARTURE_AIRPORT_ID
        expression: CORE.AIRPORT (AIRPORT_ID)
        name: FK_3
      - type: foreign_key
        columns:
          - ARRIVAL_AIRPORT_ID
        expression: CORE.AIRPORT (AIRPORT_ID)
        name: FK_4
    columns:
      - name: ROUTE_ID
        data_type: integer
        description: Unique identifier for a specific transportation route, used to distinctly label and track individual routes within a transportation system or network.
        constraints:
          - type: not_null
      - name: ROUTE_NAME
        data_type: varchar(16777216)
        description: Represents the unique name or identifier for a specific route or pathway in the transportation or navigation system. This column captures the descriptive title given to a particular route, which could be used for easy reference and identification of different travel paths.
        constraints:
          - type: not_null
      - name: AIRLINE_ID
        data_type: integer
        description: Unique identifier assigned to an airline in the database, used to distinctly represent and reference each specific airline entry
        constraints:
          - type: not_null
      - name: AIRCRAFT_ID
        data_type: integer
        description: Unique identifier assigned to each individual aircraft in the database, used to distinctly track and reference specific aircraft across various records and operations.
        constraints:
          - type: not_null
      - name: DEPARTURE_AIRPORT_ID
        data_type: integer
        description: Unique identifier for the airport from which the flight departs. This alphanumeric code represents the specific airport of origin for a flight, typically using standardized airport codes (such as IATA or ICAO codes) to precisely locate the departure point.
        constraints:
          - type: not_null
      - name: ARRIVAL_AIRPORT_ID
        data_type: integer
        description: Unique identifier for the airport where the flight or transportation unit arrives. This column likely contains airport codes (such as IATA or ICAO codes) that uniquely represent the destination airport in the transportation or flight tracking system.
        constraints:
          - type: not_null

  - name: TICKET
    description: Unique identifier for each ticket in the system, used to track and reference individual support or service requests uniquely. This ID helps in organizing, sorting, and quickly locating specific tickets within the customer support or service management workflow.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - TICKET_ID
        name: PK_1
      - type: foreign_key
        columns:
          - BOOKING_ID
        expression: CORE.BOOKING (BOOKING_ID)
        name: FK_1
    columns:
      - name: TICKET_ID
        data_type: integer
        description: Unique identifier for each ticket in the system, typically used to track and reference individual support or service requests uniquely
        constraints:
          - type: not_null
      - name: BOOKING_ID
        data_type: integer
        description: Unique identifier for each booking record, used to track and distinguish individual bookings within the system. This alphanumeric code serves as a primary key for referencing specific booking transactions.
        constraints:
          - type: not_null
      - name: CREATE_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating when the record was initially created in the database, automatically set when the record is first inserted
        constraints:
          - type: not_null
      - name: UPDATED_AT
        data_type: timestamp_ntz(9)
        description: Timestamp indicating the most recent update or modification date for this record in the table, automatically updated when any changes are made to the record.
        constraints:
          - type: not_null
      - name: BOARDING_PASS_ISSUED
        data_type: boolean
        description: Indicates whether a boarding pass has been issued for a specific travel record, typically representing the status of check-in and boarding documentation for a passenger
        constraints:
          - type: not_null

